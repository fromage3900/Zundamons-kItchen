# Open Cloud / CI secrets (never commit real values)
# Copy to .env locally — .env is gitignored.

# Roblox Open Cloud API key (CI publish / asset upload only)
ROBLOX_OPEN_CLOUD_API_KEY=

# Optional: experience / universe IDs for automation scripts
ROBLOX_UNIVERSE_ID=
ROBLOX_PLACE_ID=108617605497926

# LLM keys belong in Studio ServerStorage.ZundapalLLMSecrets — NOT here
