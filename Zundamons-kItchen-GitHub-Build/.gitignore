# Local build outputs
/workspace/

# Roblox binary place files (ignore binary, keep text format)
*.rbxl
# NOTE: *.rbxlx and *.rbxmx are NOT ignored — they are the text-format exports we commit

# OS/editor
.DS_Store
.idea/

# CI artifacts
/artifacts/

# Temp / generated files
*.lock
extract_scripts.py
rojo.zip