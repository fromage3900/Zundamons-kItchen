-- Generated by cursor_converter.py — Run in Studio command bar
local results = {}

-- Import alternate
local ok, decalId = pcall(function()
    local decal = Instance.new('Decal')
    decal.Name = 'alternate_Cursor'
    decal.Texture = 'rbxasset://Assets/Upload/Cursors/converted/alternate.png'
    return tostring(decal.Texture)
end)
if ok then
    table.insert(results, { name = 'alternate', decalId = decalId })
    print('Converted: alternate -> ' .. tostring(decalId))
end

-- Import busy
local ok, decalId = pcall(function()
    local decal = Instance.new('Decal')
    decal.Name = 'busy_Cursor'
    decal.Texture = 'rbxasset://Assets/Upload/Cursors/converted/busy.png'
    return tostring(decal.Texture)
end)
if ok then
    table.insert(results, { name = 'busy', decalId = decalId })
    print('Converted: busy -> ' .. tostring(decalId))
end

-- Import cross
local ok, decalId = pcall(function()
    local decal = Instance.new('Decal')
    decal.Name = 'cross_Cursor'
    decal.Texture = 'rbxasset://Assets/Upload/Cursors/converted/cross.png'
    return tostring(decal.Texture)
end)
if ok then
    table.insert(results, { name = 'cross', decalId = decalId })
    print('Converted: cross -> ' .. tostring(decalId))
end

-- Import handwriting
local ok, decalId = pcall(function()
    local decal = Instance.new('Decal')
    decal.Name = 'handwriting_Cursor'
    decal.Texture = 'rbxasset://Assets/Upload/Cursors/converted/handwriting.png'
    return tostring(decal.Texture)
end)
if ok then
    table.insert(results, { name = 'handwriting', decalId = decalId })
    print('Converted: handwriting -> ' .. tostring(decalId))
end

-- Import help
local ok, decalId = pcall(function()
    local decal = Instance.new('Decal')
    decal.Name = 'help_Cursor'
    decal.Texture = 'rbxasset://Assets/Upload/Cursors/converted/help.png'
    return tostring(decal.Texture)
end)
if ok then
    table.insert(results, { name = 'help', decalId = decalId })
    print('Converted: help -> ' .. tostring(decalId))
end

-- Import link
local ok, decalId = pcall(function()
    local decal = Instance.new('Decal')
    decal.Name = 'link_Cursor'
    decal.Texture = 'rbxasset://Assets/Upload/Cursors/converted/link.png'
    return tostring(decal.Texture)
end)
if ok then
    table.insert(results, { name = 'link', decalId = decalId })
    print('Converted: link -> ' .. tostring(decalId))
end

-- Import location
local ok, decalId = pcall(function()
    local decal = Instance.new('Decal')
    decal.Name = 'location_Cursor'
    decal.Texture = 'rbxasset://Assets/Upload/Cursors/converted/location.png'
    return tostring(decal.Texture)
end)
if ok then
    table.insert(results, { name = 'location', decalId = decalId })
    print('Converted: location -> ' .. tostring(decalId))
end

-- Import move
local ok, decalId = pcall(function()
    local decal = Instance.new('Decal')
    decal.Name = 'move_Cursor'
    decal.Texture = 'rbxasset://Assets/Upload/Cursors/converted/move.png'
    return tostring(decal.Texture)
end)
if ok then
    table.insert(results, { name = 'move', decalId = decalId })
    print('Converted: move -> ' .. tostring(decalId))
end

-- Import normal
local ok, decalId = pcall(function()
    local decal = Instance.new('Decal')
    decal.Name = 'normal_Cursor'
    decal.Texture = 'rbxasset://Assets/Upload/Cursors/converted/normal.png'
    return tostring(decal.Texture)
end)
if ok then
    table.insert(results, { name = 'normal', decalId = decalId })
    print('Converted: normal -> ' .. tostring(decalId))
end

-- Import person
local ok, decalId = pcall(function()
    local decal = Instance.new('Decal')
    decal.Name = 'person_Cursor'
    decal.Texture = 'rbxasset://Assets/Upload/Cursors/converted/person.png'
    return tostring(decal.Texture)
end)
if ok then
    table.insert(results, { name = 'person', decalId = decalId })
    print('Converted: person -> ' .. tostring(decalId))
end

-- Import resize_nesw
local ok, decalId = pcall(function()
    local decal = Instance.new('Decal')
    decal.Name = 'resize_nesw_Cursor'
    decal.Texture = 'rbxasset://Assets/Upload/Cursors/converted/resize_nesw.png'
    return tostring(decal.Texture)
end)
if ok then
    table.insert(results, { name = 'resize_nesw', decalId = decalId })
    print('Converted: resize_nesw -> ' .. tostring(decalId))
end

-- Import resize_ns
local ok, decalId = pcall(function()
    local decal = Instance.new('Decal')
    decal.Name = 'resize_ns_Cursor'
    decal.Texture = 'rbxasset://Assets/Upload/Cursors/converted/resize_ns.png'
    return tostring(decal.Texture)
end)
if ok then
    table.insert(results, { name = 'resize_ns', decalId = decalId })
    print('Converted: resize_ns -> ' .. tostring(decalId))
end

-- Import resize_nwse
local ok, decalId = pcall(function()
    local decal = Instance.new('Decal')
    decal.Name = 'resize_nwse_Cursor'
    decal.Texture = 'rbxasset://Assets/Upload/Cursors/converted/resize_nwse.png'
    return tostring(decal.Texture)
end)
if ok then
    table.insert(results, { name = 'resize_nwse', decalId = decalId })
    print('Converted: resize_nwse -> ' .. tostring(decalId))
end

-- Import resize_we
local ok, decalId = pcall(function()
    local decal = Instance.new('Decal')
    decal.Name = 'resize_we_Cursor'
    decal.Texture = 'rbxasset://Assets/Upload/Cursors/converted/resize_we.png'
    return tostring(decal.Texture)
end)
if ok then
    table.insert(results, { name = 'resize_we', decalId = decalId })
    print('Converted: resize_we -> ' .. tostring(decalId))
end

-- Import text
local ok, decalId = pcall(function()
    local decal = Instance.new('Decal')
    decal.Name = 'text_Cursor'
    decal.Texture = 'rbxasset://Assets/Upload/Cursors/converted/text.png'
    return tostring(decal.Texture)
end)
if ok then
    table.insert(results, { name = 'text', decalId = decalId })
    print('Converted: text -> ' .. tostring(decalId))
end

-- Import unavailable
local ok, decalId = pcall(function()
    local decal = Instance.new('Decal')
    decal.Name = 'unavailable_Cursor'
    decal.Texture = 'rbxasset://Assets/Upload/Cursors/converted/unavailable.png'
    return tostring(decal.Texture)
end)
if ok then
    table.insert(results, { name = 'unavailable', decalId = decalId })
    print('Converted: unavailable -> ' .. tostring(decalId))
end

-- Import working
local ok, decalId = pcall(function()
    local decal = Instance.new('Decal')
    decal.Name = 'working_Cursor'
    decal.Texture = 'rbxasset://Assets/Upload/Cursors/converted/working.png'
    return tostring(decal.Texture)
end)
if ok then
    table.insert(results, { name = 'working', decalId = decalId })
    print('Converted: working -> ' .. tostring(decalId))
end

print('=== RESULTS ===')
print(game:GetService('HttpService'):JSONEncode(results))