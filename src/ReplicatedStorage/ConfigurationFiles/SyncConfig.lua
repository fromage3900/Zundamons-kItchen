-- SyncConfig: visible marker so Studio Output confirms Rojo synced this repo.
-- Bump `label` when verifying a fresh sync after git pull.

return {
	label = "main-2026-07-06-org-review",
	experienceId = "108617605497926",
}
